# Evidence

Review records that back a component grade above tested. A component's entry in its package's
`registry.json` references the records here, and CI checks that each referenced file exists.

The format of a review record will be set with the grading matrix
(see [decision 0004](../decisions/0004-component-grading.md)). Records must never contain
patient information.
