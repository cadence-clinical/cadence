---
"@cadence-clinical/tokens": patch
---

Typeset: a small heading (`h5`) and a definition term (`dt`) share one style, and what follows each sits the same distance below. The space a heading owns below it now applies to every element, not only to a paragraph.
