---
"@cadence-clinical/ui": minor
---

Alert takes `emphasis`. `edge` keeps the fill, the icon and the words, and turns the outline into a bar down the leading edge, in the status's own border token, with even padding on all four sides, for a note beside what it is about. `outlined`, the default, is unchanged. The documentation site's callouts are now Cadence Alerts in that style.
