---
"@cadence-clinical/ui": minor
"@cadence-clinical/tokens": minor
---

Dialog, on the Base UI Dialog, with shadcn's part names. It is never taller than the screen, its title wraps and clears the close button, and it opens with a short fade that reduced motion keeps without the growth. The tokens gain `overlay`, a scrim that darkens the page in both modes, and assert muted text on the `popover` surface.
