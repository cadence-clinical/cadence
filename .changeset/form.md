---
"@cadence-clinical/ui": minor
---

Add Form: Base UI's Form with one error summary. A failed attempt to send lists every Field's error at the top, each linked to its control, and moves focus there. `FormSubmit` follows the form's action and cannot send it twice, and `FormActions` holds the buttons.
