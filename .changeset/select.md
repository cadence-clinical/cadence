---
"@cadence-clinical/ui": minor
---

Select, on the Base UI Select, with shadcn's part names. A chosen value and a long option wrap and are never truncated, the list opens below the field and at once, each option is as high as a control, and the list takes the field's name when it opens, which Base UI leaves unset.
