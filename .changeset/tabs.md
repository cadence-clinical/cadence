---
"@cadence-clinical/ui": minor
"@cadence-clinical/tokens": patch
---

Tabs, on the Base UI Tabs, with shadcn's part names. Tabs that do not fit wrap and are never clipped, the view shows a focus ring when it takes focus, the shown tab is marked by shape as well as colour, and nothing animates. The tokens now assert the focus ring's contrast on the `muted` surface, where a tab's ring sits.
