---
"@cadence-clinical/ui": minor
---

Add the Application shell, the first layout: a sticky header with the sidebar's button, and a Sidebar beside the page's content below it, as shadcn's `sidebar-16` block lays them out. It starts with a link that skips to the content, and the header's height follows the density.
