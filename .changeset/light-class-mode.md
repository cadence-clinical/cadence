---
"@cadence-clinical/tokens": patch
---

Treat the `light` class on `<html>` as a stated mode. A theme switcher that works by class, such as `next-themes`, states light that way, and the theme ignored it: on a dark operating system, choosing light left every token dark.
