---
"@cadence-clinical/ui": minor
---

Table, the native table elements with shadcn's part names. Cells wrap between words and a word is never broken, a table that is too wide scrolls inside a box that takes focus and is named as the table is, a heading cell has a `scope`, digits are all one width, and rows follow the density.
