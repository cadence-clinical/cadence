---
"@cadence-clinical/ui": minor
---

Add Empty, what a list or a panel shows when it has nothing in it, with shadcn's parts and props. Its spacing, text and icon tile follow the density, a link in its description shows a focus ring, and `EmptyTitle` takes `render`, so it can be a heading.
