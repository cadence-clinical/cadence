---
"@cadence-clinical/ui": minor
---

Add Drawer, a panel from an edge of the screen that can be swiped away, with shadcn's parts and props, including `showSwipeHandle`, `swipeDirection` and `snapPoints`. It uses the Dialog's scrim, scrolls what does not fit, fills its controls with its own colour, draws its handle at 3:1, and does not slide under reduced motion.
