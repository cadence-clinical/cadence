---
"@cadence-clinical/ui": patch
---

Button spaces an icon that carries `data-icon="inline-start"` or `data-icon="inline-end"`: the side that holds the icon takes less padding, so the label looks centred. Icons need no size class or margin.
