---
"@cadence-clinical/tokens": minor
"@cadence-clinical/ui": minor
---

The density scale now covers content as well as controls. `text-body`, `text-title`, `p-container` and `p-container-sm` follow `data-density`, and Card uses them, so a card's text is the size of the text in a Button inside it at both densities.
