---
"@cadence-clinical/ui": patch
---

A Data table with a sortable column no longer scrolls sideways when its columns fit. The sort button hung past the last column's padding, which put a scrollbar under every table. It now fills its heading cell, and its words line up with a plain heading's to the pixel.
