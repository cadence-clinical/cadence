---
"@cadence-clinical/tokens": minor
---

Add the severity scale: seven fixed steps, `severity-0` to `severity-6`, that an observation schema's levels take their colour from. Each step has a marker colour, a subtle fill and a border, with contrast tested in both modes and at both contrast levels. Like the status colours, no theme, accent or brand changes them.
