---
"@cadence-clinical/tokens": minor
"@cadence-clinical/ui": minor
---

Textarea, Checkbox, Radio group and Switch. The area that takes a click or a tap on a checkbox, a radio or a switch is as large as a control, so it is 44px when the density is comfortable. `size-control-indicator` joins the density scale, and `lucide-react` becomes a dependency of `@cadence-clinical/ui` for the checkbox's tick.
