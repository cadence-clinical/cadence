---
"@cadence-clinical/tokens": minor
"@cadence-clinical/ui": minor
---

Button: `size="icon"` is replaced by an `iconOnly` option that works at every size and variant. It makes the button square and hides the label from sight while keeping it as the accessible name. Button text now steps with `size` (`text-control-sm`, `text-control`, `text-control-lg`), and an icon follows the density (`size-control-icon`: 16px compact, 20px comfortable).
