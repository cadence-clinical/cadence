---
"@cadence-clinical/core": minor
"@cadence-clinical/clinical": minor
---

Observation table: rows are named by their short names, such as RR, by default, with the full name in a tooltip and for a screen reader. Set `labelStyle` to `"full"` to name them in full. A series definition and a total take a `shortLabel`, and `labelFor` gives a name in either style.
