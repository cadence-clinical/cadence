---
"@cadence-clinical/ui": minor
---

Item: a row of media, a title and a description, and actions, with shadcn's part names. Its title and description wrap and are never clamped. Spacing and text follow the density, and `ItemGroup` takes `divided` to draw lines between items as borders, which stay valid inside a `ul`.
