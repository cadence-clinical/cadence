---
"@cadence-clinical/ui": minor
---

Add Toast, with shadcn's `Toaster`, `toast` manager and parts on Base UI's Toast. Each type has the Alert's mark in its status colour, the content keeps its own height so Base UI's measuring does not loop, a toast fades rather than slides under reduced motion, and it keeps clear of a phone's home bar.
