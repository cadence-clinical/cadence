---
"@cadence-clinical/ui": minor
---

Toggle group, on the Base UI Toggle Group, following shadcn's. Its items are Toggles, so pressed is a fill and a boundary. Joined with `spacing={0}`, the pressed toggle's boundary stays whole over its neighbours'. `variant`, `size` and `iconOnly` are set once on the group, and a group that does not fit wraps.
