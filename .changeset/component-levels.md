---
"@cadence-clinical/ui": patch
---

Every component now has a level (primitive, composite, pattern or layout) and a domain (general or clinical). In `@cadence-clinical/ui/meta.json` each entry's `category` is replaced by `level` and `domain`.
