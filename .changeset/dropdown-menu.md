---
"@cadence-clinical/ui": minor
"@cadence-clinical/tokens": patch
---

Dropdown menu, on the Base UI Menu, with shadcn's part names except the submenu parts, which are left out. Each item is as high as a control, a checked or chosen item carries a tick, a long item wraps, the menu opens at once, and a label works outside a group as it does in shadcn's blocks. The tokens now assert each status's text colour on the `popover` surface.
