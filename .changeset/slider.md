---
"@cadence-clinical/ui": minor
---

Add Slider, for a setting chosen from a range. The strip it runs along is a target as high as a control, the unfilled track is the input colour so the whole range can be seen at 3:1, a single value has one thumb, and `thumbLabels` names each thumb of a range.
